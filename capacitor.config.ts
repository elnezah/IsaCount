import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'eu.elnezah.isacount',
  appName: 'IsaCount',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
